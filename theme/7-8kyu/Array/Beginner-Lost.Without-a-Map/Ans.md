> Если задан массив целых чисел, верните новый массив, каждое значение которого удвоено.

```js
function maps(x) {
  return x.map((n) => n * 2);
}

// ----------------------------------------

maps = (x) => x.map((e) => e * 2);
```
